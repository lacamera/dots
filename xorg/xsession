#!/bin/sh
cfg="${XDG_CONFIG_HOME:-"$HOME/.config"}"
env="$cfg/env"
Xresources="$cfg/xresources"

[[ -f $Xresources ]] && . $env
[[ -f $Xresources ]] && xrdb -load $Xresources

xset b off
xset r rate 256 32

xhi

nm-applet &
blueberry-tray &
pasystray &
exec i3
